// examples/demo.ltl — runnable demo for ltl_irc_bot

import std::io
use ltl_irc_bot as lib

fn main() {
    io::println("ltl_irc_bot demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}